<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
<title>MultiMatch — Find the best price. Track every order.</title>
<meta name="description" content="Search across major retailers to compare prices, ratings, and delivery. Track all your online orders in one place.">
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Syne:wght@500;600;700;800&family=DM+Sans:wght@300;400;500;600;700&display=swap" rel="stylesheet">
<style>
:root {
  --bg: #08080f;
  --surface: #111118;
  --surface2: #18181f;
  --surface3: #1f1f28;
  --border: #26263a;
  --border2: #32324a;
  --accent: #00e5a0;
  --accent-dim: rgba(0,229,160,0.12);
  --accent2: #7c6dfa;
  --accent2-dim: rgba(124,109,250,0.12);
  --accent3: #ff6b6b;
  --accent3-dim: rgba(255,107,107,0.12);
  --yellow: #f5c842;
  --text: #eeeef8;
  --text2: #b0b0cc;
  --muted: #7b7b96;
  --card: #13131c;
  --radius: 14px;
  --radius-sm: 10px;
  --shadow: 0 12px 40px rgba(0,0,0,0.35);
  --transition: 0.2s cubic-bezier(0.4,0,0.2,1);
}
* { margin:0; padding:0; box-sizing:border-box; -webkit-tap-highlight-color:transparent; }
html { scroll-behavior:smooth; }
body {
  font-family:'DM Sans',sans-serif;
  background:var(--bg);
  color:var(--text);
  min-height:100dvh;
  display:flex;
  flex-direction:column;
  max-width:480px;
  margin:0 auto;
  line-height:1.45;
  overscroll-behavior:none;
  -webkit-font-smoothing:antialiased;
  position:relative;
}
a { color:inherit; text-decoration:none; }
button,input,select,textarea { font:inherit; color:inherit; }

/* Top Bar */
.top-bar {
  position:sticky; top:0; z-index:100;
  background:rgba(8,8,15,0.95);
  backdrop-filter:blur(20px); -webkit-backdrop-filter:blur(20px);
  border-bottom:1px solid var(--border);
  display:flex; align-items:center; justify-content:space-between;
  padding:0 16px; height:56px; flex-shrink:0;
}
.logo {
  display:flex; align-items:center; gap:8px;
  font-family:'Syne',sans-serif; font-weight:800; font-size:1.05rem;
  letter-spacing:-0.03em;
}
.logo-mark {
  width:28px; height:28px;
  background:linear-gradient(135deg,var(--accent),var(--accent2));
  border-radius:7px; display:flex; align-items:center; justify-content:center;
  font-size:12px; box-shadow:0 4px 12px rgba(0,229,160,0.25);
}
.logo em { font-style:normal; color:var(--accent); }
.top-bar-actions { display:flex; align-items:center; gap:6px; }
.icon-btn {
  width:38px; height:38px; background:var(--surface2);
  border:1px solid var(--border); border-radius:8px;
  display:flex; align-items:center; justify-content:center;
  cursor:pointer; color:var(--text2); font-size:0.9rem;
  transition:var(--transition); position:relative;
}
.icon-btn:hover,.icon-btn:focus-visible { border-color:var(--border2); color:var(--text); outline:none; }
.icon-btn:active { transform:scale(0.95); }
.notif-dot {
  position:absolute; top:6px; right:6px; width:6px; height:6px;
  background:var(--accent3); border-radius:50%; border:1.5px solid var(--bg);
}

/* Main Content */
.main { flex:1; overflow-y:auto; padding-bottom:90px; }
.page { display:none; animation:fadeIn 0.25s ease; }
.page.active { display:block; }
@keyframes fadeIn { from{opacity:0;transform:translateY(6px);} to{opacity:1;transform:translateY(0);} }

/* Tab Bar */
.tab-bar {
  position:fixed; bottom:0; left:50%; transform:translateX(-50%);
  width:100%; max-width:480px;
  background:rgba(8,8,15,0.97);
  backdrop-filter:blur(20px); -webkit-backdrop-filter:blur(20px);
  border-top:1px solid var(--border);
  display:flex; z-index:100; padding:6px 0 env(safe-area-inset-bottom,8px);
}
.tab {
  flex:1; display:flex; flex-direction:column; align-items:center; gap:2px;
  cursor:pointer; padding:4px 0; border:none; background:none;
  transition:var(--transition); position:relative;
}
.tab-icon { font-size:1.15rem; line-height:1; }
.tab-label { font-size:0.65rem; font-weight:600; color:var(--muted); transition:var(--transition); }
.tab.active .tab-label { color:var(--accent); }
.tab-pip {
  position:absolute; top:0; width:16px; height:2px;
  background:var(--accent); border-radius:0 0 2px 2px; opacity:0; transition:var(--transition);
}
.tab.active .tab-pip { opacity:1; }
.tab:focus-visible,.icon-btn:focus-visible,.btn:focus-visible,.filter-chip:focus-visible {
  outline:2px solid var(--accent); outline-offset:2px;
}

/* Typography */
.section-label {
  font-size:0.68rem; text-transform:uppercase; letter-spacing:0.1em;
  color:var(--muted); font-weight:700; margin-bottom:10px;
}

/* Buttons */
.btn {
  display:flex; align-items:center; justify-content:center; gap:6px;
  border:none; border-radius:var(--radius-sm);
  font-family:'Syne',sans-serif; font-weight:700; font-size:0.9rem;
  padding:13px 18px; cursor:pointer; transition:var(--transition);
  width:100%; letter-spacing:-0.01em; min-height:44px;
}
.btn:active { transform:scale(0.98); }
.btn-primary {
  background:linear-gradient(135deg,var(--accent),#00c896);
  color:#000; box-shadow:0 4px 16px rgba(0,229,160,0.22);
}
.btn-primary:hover { box-shadow:0 6px 24px rgba(0,229,160,0.35); }
.btn-secondary { background:var(--surface2); border:1px solid var(--border); color:var(--text); }
.btn-ghost { background:none; border:1px solid var(--border); color:var(--text2); font-family:'DM Sans',sans-serif; font-weight:600; }
.btn-sm {
  font-size:0.78rem; padding:8px 14px; min-height:36px;
  border-radius:8px; font-family:'Syne',sans-serif; font-weight:700;
  cursor:pointer; transition:var(--transition); border:none;
}

/* Cards */
.card {
  background:var(--card); border:1px solid var(--border);
  border-radius:var(--radius); padding:16px; margin-bottom:10px;
}
.card-header {
  display:flex; align-items:center; justify-content:space-between; margin-bottom:12px;
}
.card-header h3 { font-family:'Syne',sans-serif; font-weight:700; font-size:0.9rem; }

/* Forms */
input[type="text"],input[type="search"],input[type="email"],select,textarea {
  width:100%; background:var(--surface2); border:1px solid var(--border);
  border-radius:var(--radius-sm); color:var(--text);
  font-size:0.9rem; padding:12px 14px; outline:none;
  transition:border-color var(--transition),box-shadow var(--transition);
  -webkit-appearance:none; appearance:none;
}
input:focus,select:focus,textarea:focus {
  border-color:var(--accent); box-shadow:0 0 0 3px rgba(0,229,160,0.08);
}
input::placeholder,textarea::placeholder { color:var(--muted); opacity:0.7; }
select {
  background-image:url("data:image/svg+xml,%3Csvg width='10' height='6' viewBox='0 0 10 6' fill='none' xmlns='http://www.w3.org/2000/svg'%3E%3Cpath d='M1 1L5 5L9 1' stroke='%237b7b96' stroke-width='1.5' stroke-linecap='round' stroke-linejoin='round'/%3E%3C/svg%3E");
  background-repeat:no-repeat; background-position:right 12px center;
  padding-right:32px; cursor:pointer;
}
label.field-label {
  display:block; font-size:0.7rem; font-weight:700; text-transform:uppercase;
  letter-spacing:0.08em; color:var(--muted); margin-bottom:6px;
}

/* Pills */
.pill {
  display:inline-flex; align-items:center; gap:3px;
  font-size:0.68rem; font-weight:700; padding:3px 8px; border-radius:16px;
}
.pill-green { background:var(--accent-dim); color:var(--accent); border:1px solid rgba(0,229,160,0.2); }
.pill-purple { background:var(--accent2-dim); color:var(--accent2); border:1px solid rgba(124,109,250,0.2); }
.pill-red { background:var(--accent3-dim); color:var(--accent3); border:1px solid rgba(255,107,107,0.2); }
.pill-yellow { background:rgba(245,200,66,0.12); color:var(--yellow); border:1px solid rgba(245,200,66,0.2); }

/* Toast */
.toast {
  position:fixed; bottom:100px; left:50%; transform:translateX(-50%) translateY(10px);
  background:var(--surface); border:1px solid var(--border2);
  color:var(--text); font-size:0.82rem; font-weight:600;
  padding:10px 18px; border-radius:24px; z-index:999;
  box-shadow:0 8px 32px rgba(0,0,0,0.4);
  opacity:0; transition:var(--transition); pointer-events:none; white-space:nowrap;
}
.toast.show { opacity:1; transform:translateX(-50%) translateY(0); }

/* ========== FIND PAGE ========== */
#page-find { padding:16px; }
.find-hero { margin-bottom:18px; }
.find-hero h1 {
  font-family:'Syne',sans-serif; font-weight:800; font-size:1.5rem;
  line-height:1.15; letter-spacing:-0.03em; margin-bottom:6px;
}
.find-hero h1 em {
  font-style:normal;
  background:linear-gradient(90deg,var(--accent),var(--accent2));
  -webkit-background-clip:text; -webkit-text-fill-color:transparent; background-clip:text;
}
.find-hero p { font-size:0.85rem; color:var(--text2); line-height:1.5; }
.search-card {
  background:var(--surface); border:1px solid var(--border);
  border-radius:16px; padding:14px; margin-bottom:12px;
}
.field-group { margin-bottom:10px; }
.search-row { display:grid; grid-template-columns:1fr auto; gap:8px; align-items:end; }
.photo-btn {
  background:var(--surface2); border:1.5px dashed var(--border2);
  border-radius:var(--radius-sm); color:var(--text2);
  font-size:0.78rem; font-weight:600; padding:12px 12px;
  cursor:pointer; transition:var(--transition);
  display:flex; align-items:center; gap:5px; white-space:nowrap; min-height:44px;
}
.photo-btn:hover { border-color:var(--accent); color:var(--accent); }
.how-section { margin-bottom:18px; }
.steps-grid { display:grid; grid-template-columns:1fr 1fr 1fr; gap:8px; }
.step-card {
  background:var(--surface); border:1px solid var(--border);
  border-radius:var(--radius); padding:14px; text-align:center;
}
.step-num {
  width:28px; height:28px; margin:0 auto 8px;
  background:linear-gradient(135deg,var(--accent2),var(--accent));
  border-radius:7px; display:flex; align-items:center; justify-content:center;
  font-family:'Syne',sans-serif; font-weight:800; font-size:0.8rem; color:#fff;
}
.step-card h4 { font-family:'Syne',sans-serif; font-weight:700; font-size:0.8rem; margin-bottom:3px; }
.step-card p { font-size:0.72rem; color:var(--muted); line-height:1.4; }

/* ========== RESULTS PAGE ========== */
#page-results { padding:16px; }
.results-header {
  display:flex; align-items:center; gap:10px; margin-bottom:12px;
}
.back-btn {
  width:38px; height:38px; background:var(--surface);
  border:1px solid var(--border); border-radius:8px;
  display:flex; align-items:center; justify-content:center;
  cursor:pointer; font-size:1rem; flex-shrink:0;
  transition:var(--transition); color:var(--text2);
}
.back-btn:hover { border-color:var(--border2); color:var(--text); }
.results-header h2 {
  font-family:'Syne',sans-serif; font-weight:700; font-size:0.95rem;
  letter-spacing:-0.02em; flex:1; overflow:hidden; text-overflow:ellipsis; white-space:nowrap;
}
.filter-row {
  display:flex; gap:6px; overflow-x:auto; padding-bottom:4px;
  margin-bottom:12px; scrollbar-width:none;
}
.filter-row::-webkit-scrollbar { display:none; }
.filter-chip {
  flex-shrink:0; background:var(--surface); border:1px solid var(--border);
  border-radius:18px; padding:7px 12px; font-size:0.72rem;
  color:var(--text2); white-space:nowrap; cursor:pointer;
  font-weight:600; transition:var(--transition);
}
.filter-chip.active,.filter-chip:hover { background:var(--accent-dim); border-color:rgba(0,229,160,0.3); color:var(--accent); }

/* Best Match */
.best-card {
  background:linear-gradient(135deg,rgba(0,229,160,0.06),rgba(124,109,250,0.04));
  border:1px solid rgba(0,229,160,0.2); border-radius:var(--radius);
  padding:14px; margin-bottom:10px;
}
.best-tag {
  display:inline-flex; align-items:center; gap:3px;
  background:var(--accent); color:#000;
  font-family:'Syne',sans-serif; font-size:0.62rem; font-weight:800;
  padding:2px 8px; border-radius:16px; text-transform:uppercase;
  letter-spacing:0.05em; margin-bottom:8px;
}
.best-row { display:flex; align-items:center; gap:10px; }
.best-img {
  width:48px; height:48px; background:var(--surface2); border-radius:8px;
  display:flex; align-items:center; justify-content:center;
  font-size:1.5rem; flex-shrink:0;
}
.best-info { flex:1; min-width:0; }
.best-info h3 { font-family:'Syne',sans-serif; font-weight:700; font-size:0.85rem; margin-bottom:2px; }
.best-info p { font-size:0.72rem; color:var(--text2); }
.best-price {
  font-family:'Syne',sans-serif; font-weight:800; font-size:1.3rem;
  color:var(--accent); letter-spacing:-0.03em; text-align:right; flex-shrink:0;
}
.best-sub { font-size:0.68rem; color:var(--muted); text-align:right; }
.best-buy-btn {
  display:block; width:100%; margin-top:10px;
  background:var(--accent); border:none; border-radius:var(--radius-sm);
  color:#000; font-family:'Syne',sans-serif; font-weight:700;
  font-size:0.85rem; padding:11px; cursor:pointer; transition:var(--transition);
  text-align:center; min-height:44px;
}
.best-buy-btn:hover { box-shadow:0 4px 16px rgba(0,229,160,0.3); }

/* Result Card */
.result-card {
  background:var(--card); border:1px solid var(--border);
  border-radius:var(--radius); padding:12px; margin-bottom:8px;
  display:grid; grid-template-columns:44px 1fr auto; gap:10px;
  align-items:center; cursor:pointer; transition:var(--transition);
}
.result-card:hover { border-color:var(--border2); }
.r-img {
  width:44px; height:44px; background:var(--surface2);
  border-radius:8px; display:flex; align-items:center;
  justify-content:center; font-size:1.3rem;
}
.r-info h4 { font-family:'Syne',sans-serif; font-weight:700; font-size:0.8rem; margin-bottom:2px; }
.r-retailer { font-size:0.68rem; color:var(--muted); }
.r-stars { font-size:0.68rem; color:var(--yellow); }
.r-stars span { color:var(--muted); }
.r-tags { display:flex; gap:4px; margin-top:4px; flex-wrap:wrap; }
.r-price-col { text-align:right; }
.r-was { font-size:0.65rem; color:var(--muted); text-decoration:line-through; }
.r-price {
  font-family:'Syne',sans-serif; font-weight:800; font-size:1.05rem;
  letter-spacing:-0.02em;
}
.r-price.best { color:var(--accent); }
.r-btn {
  background:var(--surface2); border:1px solid var(--border);
  border-radius:6px; color:var(--text); font-size:0.7rem;
  font-weight:700; padding:6px 10px; cursor:pointer;
  transition:var(--transition); white-space:nowrap; min-height:32px;
  font-family:'Syne',sans-serif;
}
.r-btn:hover { border-color:var(--border2); }
.r-btn.primary { background:var(--accent); border-color:var(--accent); color:#000; }

/* ========== ORDERS PAGE ========== */
#page-orders { padding:16px; }
.orders-header {
  display:flex; align-items:center; justify-content:space-between; margin-bottom:16px;
}
.orders-header h2 { font-family:'Syne',sans-serif; font-weight:800; font-size:1.3rem; letter-spacing:-0.02em; }
.order-filters { display:flex; gap:6px; margin-bottom:14px; overflow-x:auto; scrollbar-width:none; }
.order-filters::-webkit-scrollbar { display:none; }
.order-filter {
  flex-shrink:0; background:var(--surface); border:1px solid var(--border);
  border-radius:18px; padding:7px 14px; font-size:0.73rem;
  color:var(--text2); cursor:pointer; font-weight:600;
  transition:var(--transition);
}
.order-filter.active,.order-filter:hover { background:var(--accent-dim); border-color:rgba(0,229,160,0.3); color:var(--accent); }
.order-stats {
  display:grid; grid-template-columns:repeat(3,1fr); gap:8px; margin-bottom:14px;
}
.stat-card {
  background:var(--surface); border:1px solid var(--border);
  border-radius:var(--radius); padding:12px; text-align:center;
}
.stat-card strong { display:block; font-family:'Syne',sans-serif; font-size:1.2rem; }
.stat-card span { font-size:0.7rem; color:var(--muted); }
.order-card {
  background:var(--card); border:1px solid var(--border);
  border-radius:var(--radius); padding:14px; margin-bottom:10px;
}
.order-card-header {
  display:flex; align-items:center; justify-content:space-between; margin-bottom:10px;
}
.order-retailer {
  display:flex; align-items:center; gap:8px;
}
.order-retailer-icon {
  width:32px; height:32px; background:var(--surface2);
  border-radius:6px; display:flex; align-items:center;
  justify-content:center; font-size:1rem;
}
.order-retailer-name { font-family:'Syne',sans-serif; font-weight:700; font-size:0.82rem; }
.order-status { font-size:0.7rem; }
.order-items { font-size:0.8rem; color:var(--text2); margin-bottom:10px; line-height:1.5; }
.order-progress {
  background:var(--surface2); border-radius:8px; height:4px;
  margin-bottom:10px; overflow:hidden;
}
.order-progress-bar {
  height:100%; border-radius:8px; transition:width 0.5s ease;
}
.order-progress-bar.shipped { background:var(--accent2); }
.order-progress-bar.delivered { background:var(--accent); }
.order-progress-bar.processing { background:var(--yellow); }
.order-footer {
  display:flex; align-items:center; justify-content:space-between;
  font-size:0.72rem; color:var(--muted);
}
.order-actions { display:flex; gap:6px; }

/* ========== ACCOUNT PAGE ========== */
#page-account { padding:16px; }
.account-header { text-align:center; margin-bottom:20px; }
.avatar {
  width:64px; height:64px; border-radius:50%;
  background:linear-gradient(135deg,var(--accent),var(--accent2));
  display:flex; align-items:center; justify-content:center;
  font-size:1.8rem; margin:0 auto 10px;
}
.account-header h2 { font-family:'Syne',sans-serif; font-weight:800; font-size:1.2rem; }
.account-header p { font-size:0.8rem; color:var(--muted); }
.menu-item {
  display:flex; align-items:center; gap:12px; padding:14px 0;
  border-bottom:1px solid var(--border); cursor:pointer;
  transition:var(--transition);
}
.menu-item:hover { opacity:0.8; }
.menu-item-icon {
  width:36px; height:36px; background:var(--surface2);
  border-radius:8px; display:flex; align-items:center;
  justify-content:center; font-size:1rem;
}
.menu-item-text { flex:1; }
.menu-item-text strong { display:block; font-size:0.85rem; font-family:'Syne',sans-serif; font-weight:600; }
.menu-item-text span { font-size:0.72rem; color:var(--muted); }
.menu-item-arrow { color:var(--muted); font-size:0.8rem; }
.disclosure-box {
  background:var(--accent2-dim); border:1px solid rgba(124,109,250,0.2);
  border-radius:var(--radius-sm); padding:12px 14px;
  font-size:0.75rem; color:var(--muted); line-height:1.6; margin-top:16px;
}
.disclosure-box strong { color:var(--accent2); }

/* Loading & Empty */
.loading,.empty-state {
  text-align:center; padding:24px; color:var(--muted);
}
.spinner {
  width:28px; height:28px; border:3px solid var(--border);
  border-top-color:var(--accent); border-radius:50%;
  animation:spin 0.8s linear infinite; margin:0 auto 12px;
}
@keyframes spin { to{transform:rotate(360deg);} }
.skeleton {
  height:14px; background:linear-gradient(90deg,var(--surface2),var(--surface3),var(--surface2));
  background-size:200% 100%; animation:shimmer 1.25s infinite;
  border-radius:6px; margin:6px 0;
}
@keyframes shimmer { 0%{background-position:200% 0;} 100%{background-position:-200% 0;} }

@media (prefers-reduced-motion:reduce) {
  *{animation-duration:0.01ms!important;transition-duration:0.01ms!important;}
}
</style>
</head>
<body>

<!-- Top Bar -->
<header class="top-bar">
  <a href="#" class="logo" onclick="navigateTo('find')">
    <span class="logo-mark">◎</span>
    <span>Multi<em>Match</em></span>
  </a>
  <div class="top-bar-actions">
    <button class="icon-btn" aria-label="Notifications">🔔<span class="notif-dot"></span></button>
    <button class="icon-btn" aria-label="Saved items" onclick="navigateTo('account')">♡</button>
  </div>
</header>

<!-- Main Content -->
<main class="main" id="main">

  <!-- FIND PAGE -->
  <section class="page active" id="page-find">
    <div class="find-hero">
      <h1>Find the best price.<br><em>Track every order.</em></h1>
      <p>Search across retailers, compare, and track all your deliveries.</p>
    </div>

    <div class="search-card">
      <div class="field-group">
        <label class="field-label" for="searchQuery">What are you looking for?</label>
        <input type="search" id="searchQuery" placeholder="e.g. Sony WH-1000XM5 headphones" autocomplete="off">
      </div>
      <div class="field-group">
        <label class="field-label" for="searchCategory">Category</label>
        <select id="searchCategory">
          <option value="">Any category</option>
          <option>Electronics</option><option>Home & Garden</option>
          <option>Fashion</option><option>Beauty</option>
          <option>Fitness</option><option>Groceries</option>
        </select>
      </div>
      <div class="search-row">
        <button class="photo-btn" onclick="showToast('📷 Photo search coming soon')">📷 Scan barcode</button>
        <button class="btn btn-primary" onclick="searchProducts()" style="width:auto;">Search</button>
      </div>
    </div>

    <div class="how-section">
      <div class="section-label">How it works</div>
      <div class="steps-grid">
        <div class="step-card">
          <div class="step-num">1</div>
          <h4>Search</h4>
          <p>Product, photo, or barcode</p>
        </div>
        <div class="step-card">
          <div class="step-num">2</div>
          <h4>Compare</h4>
          <p>Price, rating & delivery</p>
        </div>
        <div class="step-card">
          <div class="step-num">3</div>
          <h4>Track</h4>
          <p>All orders in one place</p>
        </div>
      </div>
    </div>

    <div class="section-label">Recent searches</div>
    <div class="filter-row">
      <div class="filter-chip">Sony headphones</div>
      <div class="filter-chip">Nike running shoes</div>
      <div class="filter-chip">iPhone charger</div>
      <div class="filter-chip">Protein powder</div>
    </div>
  </section>

  <!-- RESULTS PAGE -->
  <section class="page" id="page-results">
    <div class="results-header">
      <button class="back-btn" onclick="navigateTo('find')">←</button>
      <h2 id="resultsTitle">Results for "Sony headphones"</h2>
    </div>
    <div class="filter-row" id="resultFilters">
      <button class="filter-chip active" onclick="filterResults(this,'all')">All</button>
      <button class="filter-chip" onclick="filterResults(this,'price')">Best price</button>
      <button class="filter-chip" onclick="filterResults(this,'delivery')">Fast delivery</button>
      <button class="filter-chip" onclick="filterResults(this,'rated')">Top rated</button>
    </div>

    <!-- Best Match -->
    <div class="best-card">
      <div class="best-tag">🥇 Best match</div>
      <div class="best-row">
        <div class="best-img">🎧</div>
        <div class="best-info">
          <h3>Sony WH-1000XM5</h3>
          <p>Best noise cancelling • 30hr battery</p>
        </div>
        <div>
          <div class="best-price">A$379</div>
          <div class="best-sub">Save A$70</div>
        </div>
      </div>
      <button class="best-buy-btn" onclick="showToast('🔗 Opening retailer...')">View best deal →</button>
    </div>

    <!-- Result Cards -->
    <div id="resultsList"></div>
  </section>

  <!-- ORDERS PAGE -->
  <section class="page" id="page-orders">
    <div class="orders-header">
      <h2>Your orders</h2>
      <button class="btn-sm btn-secondary" onclick="showToast('✉️ Connect email coming soon')">+ Add order</button>
    </div>

    <div class="order-filters">
      <button class="order-filter active" onclick="filterOrders(this,'all')">All</button>
      <button class="order-filter" onclick="filterOrders(this,'active')">Active</button>
      <button class="order-filter" onclick="filterOrders(this,'delivered')">Delivered</button>
      <button class="order-filter" onclick="filterOrders(this,'delayed')">Delayed</button>
    </div>

    <div class="order-stats">
      <div class="stat-card">
        <strong>3</strong>
        <span>Active orders</span>
      </div>
      <div class="stat-card">
        <strong>12</strong>
        <span>Delivered</span>
      </div>
      <div class="stat-card">
        <strong>A$847</strong>
        <span>This month</span>
      </div>
    </div>

    <div id="ordersList"></div>
  </section>

  <!-- ACCOUNT PAGE -->
  <section class="page" id="page-account">
    <div class="account-header">
      <div class="avatar">👤</div>
      <h2>Welcome back</h2>
      <p>Track orders & save your searches</p>
    </div>

    <div class="card" style="margin-bottom:16px;">
      <div class="menu-item" onclick="showToast('🔔 Price alerts coming soon')">
        <div class="menu-item-icon">🔔</div>
        <div class="menu-item-text">
          <strong>Price alerts</strong>
          <span>3 active alerts</span>
        </div>
        <div class="menu-item-arrow">›</div>
      </div>
      <div class="menu-item" onclick="showToast('♡ Wishlist coming soon')">
        <div class="menu-item-icon">♡</div>
        <div class="menu-item-text">
          <strong>Saved items</strong>
          <span>5 products saved</span>
        </div>
        <div class="menu-item-arrow">›</div>
      </div>
      <div class="menu-item" onclick="showToast('📊 Analytics coming soon')">
        <div class="menu-item-icon">📊</div>
        <div class="menu-item-text">
          <strong>Spend insights</strong>
          <span>Monthly breakdowns</span>
        </div>
        <div class="menu-item-arrow">›</div>
      </div>
      <div class="menu-item" onclick="showToast('⚙️ Settings coming soon')">
        <div class="menu-item-icon">⚙️</div>
        <div class="menu-item-text">
          <strong>Settings</strong>
          <span>Notifications, preferences</span>
        </div>
        <div class="menu-item-arrow">›</div>
      </div>
    </div>

    <div class="disclosure-box">
      <strong>Disclosure:</strong> MultiMatch may earn a commission when you make a purchase through our links, at no extra cost to you. This does not affect our rankings — we always show the genuinely best deal first. <a href="how-we-make-money.html" style="text-decoration:underline;color:var(--accent2);">Learn more</a>
    </div>
  </section>

</main>

<!-- Tab Bar -->
<nav class="tab-bar" role="navigation" aria-label="Main navigation">
  <button class="tab active" onclick="navigateTo('find')" aria-label="Search products">
    <span class="tab-icon">🔍</span>
    <span class="tab-label">Find</span>
    <span class="tab-pip"></span>
  </button>
  <button class="tab" onclick="navigateTo('orders')" aria-label="Your orders">
    <span class="tab-icon">📦</span>
    <span class="tab-label">Orders</span>
    <span class="tab-pip"></span>
  </button>
  <button class="tab" onclick="navigateTo('account')" aria-label="Account">
    <span class="tab-icon">👤</span>
    <span class="tab-label">Account</span>
    <span class="tab-pip"></span>
  </button>
</nav>

<!-- Toast -->
<div class="toast" id="toast" role="status" aria-live="polite"></div>

<script>
// ============ NAVIGATION ============
let currentPage = 'find';

function navigateTo(page) {
  document.querySelectorAll('.page').forEach(p => p.classList.remove('active'));
  document.querySelectorAll('.tab').forEach(t => t.classList.remove('active'));
  
  const pageEl = document.getElementById('page-' + page);
  if (pageEl) pageEl.classList.add('active');
  
  const tabMap = { find:0, orders:1, account:2 };
  const tabs = document.querySelectorAll('.tab');
  if (tabs[tabMap[page]]) tabs[tabMap[page]].classList.add('active');
  
  currentPage = page;
  document.getElementById('main').scrollTop = 0;
}

// ============ SEARCH ============
function searchProducts() {
  const query = document.getElementById('searchQuery').value.trim();
  if (!query) {
    showToast('Please enter a search term');
    return;
  }
  document.getElementById('resultsTitle').textContent = `Results for "${query}"`;
  loadResults(query);
  navigateTo('results');
}

function loadResults(query) {
  const list = document.getElementById('resultsList');
  list.innerHTML = '<div class="loading"><div class="spinner"></div><p>Searching retailers...</p></div>';
  
  // Simulated API call
  setTimeout(() => {
    const results = getMockResults(query);
    list.innerHTML = results.map((r, i) => `
      <div class="result-card" onclick="showToast('🔗 Opening ${r.retailer}...')">
        <div class="r-img">${r.icon}</div>
        <div class="r-info">
          <h4>${r.name}</h4>
          <div class="r-retailer">${r.retailer} · ${r.delivery}</div>
          <div class="r-stars">${'★'.repeat(Math.floor(r.rating))}${r.rating % 1 ? '½' : ''} <span>${r.rating}</span></div>
          <div class="r-tags">
            ${r.tags.map(t => `<span class="pill pill-${t.color}">${t.label}</span>`).join('')}
          </div>
        </div>
        <div class="r-price-col">
          ${r.was ? `<div class="r-was">A$${r.was}</div>` : ''}
          <div class="r-price ${i===0 ? 'best' : ''}">A$${r.price}</div>
          <button class="r-btn ${i===0 ? 'primary' : ''}" onclick="event.stopPropagation();showToast('🔗 Opening ${r.retailer}...')">View</button>
        </div>
      </div>
    `).join('');
  }, 800);
}

function getMockResults(query) {
  return [
    { icon:'🎧', name:'Sony WH-1000XM5 Wireless', retailer:'Amazon AU', delivery:'Free · Tomorrow', rating:4.8, price:379, was:449, tags:[{label:'Best match',color:'green'},{label:'Free ship',color:'purple'}] },
    { icon:'🎧', name:'Sony WH-1000XM5 (Silver)', retailer:'JB Hi-Fi', delivery:'Click & collect', rating:4.7, price:389, was:449, tags:[{label:'In stock',color:'green'}] },
    { icon:'🎧', name:'Sony WH-1000XM5', retailer:'eBay AU', delivery:'3-5 days', rating:4.3, price:355, was:null, tags:[{label:'Refurb',color:'yellow'}] },
    { icon:'🎧', name:'Sony WH-1000XM5 Bundle', retailer:'Kogan', delivery:'Free · 2 days', rating:4.6, price:395, was:499, tags:[{label:'Bundle',color:'purple'},{label:'Sale',color:'red'}] },
    { icon:'🎧', name:'Sony WH-1000XM4 (Prev gen)', retailer:'Amazon AU', delivery:'Free · Tomorrow', rating:4.7, price:269, was:349, tags:[{label:'Value pick',color:'green'},{label:'Save A$80',color:'red'}] },
    { icon:'🎧', name:'Sony WH-1000XM5', retailer:'Officeworks', delivery:'Click & collect', rating:4.5, price:399, was:449, tags:[{label:'Price match',color:'purple'}] },
  ];
}

function filterResults(btn, filter) {
  document.querySelectorAll('#resultFilters .filter-chip').forEach(c => c.classList.remove('active'));
  btn.classList.add('active');
  showToast(`Filtering by ${filter}...`);
  loadResults(document.getElementById('searchQuery').value || 'headphones');
}

// ============ ORDERS ============
function loadOrders() {
  const list = document.getElementById('ordersList');
  const orders = [
    { retailer:'Amazon AU', icon:'📦', items:'Sony WH-1000XM5 Headphones', price:'A$379', status:'Out for delivery', progress:75, progressClass:'shipped', eta:'Today by 6pm', date:'Ordered May 20' },
    { retailer:'JB Hi-Fi', icon:'🎮', items:'Nintendo Switch Pro Controller', price:'A$89', status:'Shipped', progress:50, progressClass:'shipped', eta:'May 25-27', date:'Ordered May 21' },
    { retailer:'eBay AU', icon:'👕', items:'Nike Running Shoes + 2 tops', price:'A$156', status:'Processing', progress:15, progressClass:'processing', eta:'Jun 2-8', date:'Ordered May 22' },
    { retailer:'Kogan', icon:'📱', items:'iPhone USB-C Charger 2-pack', price:'A$32', status:'Delivered', progress:100, progressClass:'delivered', eta:'Delivered May 18', date:'Ordered May 15' },
  ];
  
  list.innerHTML = orders.map(o => `
    <div class="order-card">
      <div class="order-card-header">
        <div class="order-retailer">
          <div class="order-retailer-icon">${o.icon}</div>
          <div>
            <div class="order-retailer-name">${o.retailer}</div>
            <div class="order-status pill pill-${o.progressClass==='delivered'?'green':o.progressClass==='shipped'?'purple':'yellow'}">${o.status}</div>
          </div>
        </div>
        <span style="font-family:'Syne',sans-serif;font-weight:700;font-size:0.85rem;">${o.price}</span>
      </div>
      <div class="order-items">${o.items}</div>
      <div class="order-progress">
        <div class="order-progress-bar ${o.progressClass}" style="width:${o.progress}%;"></div>
      </div>
      <div class="order-footer">
        <span>${o.date}</span>
        <span><strong>${o.eta}</strong></span>
      </div>
      <div class="order-actions" style="margin-top:10px;">
        <button class="btn-sm btn-ghost" onclick="showToast('🔍 Tracking details...')">Track</button>
        <button class="btn-sm btn-primary" onclick="showToast('🔄 Finding best price to reorder...')">Reorder</button>
      </div>
    </div>
  `).join('');
}

function filterOrders(btn, filter) {
  document.querySelectorAll('.order-filter').forEach(c => c.classList.remove('active'));
  btn.classList.add('active');
  showToast(`Showing ${filter} orders`);
  loadOrders();
}

// ============ TOAST ============
function showToast(msg) {
  const toast = document.getElementById('toast');
  toast.textContent = msg;
  toast.classList.add('show');
  clearTimeout(toast._t);
  toast._t = setTimeout(() => toast.classList.remove('show'), 2500);
}

// ============ INIT ============
document.addEventListener('DOMContentLoaded', () => {
  loadOrders();
  
  // Search on Enter
  document.getElementById('searchQuery').addEventListener('keydown', (e) => {
    if (e.key === 'Enter') searchProducts();
  });
});
</script>
</body>
</html>